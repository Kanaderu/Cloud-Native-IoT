services:
  idf:
    image: espressif/idf:v5.4.2
    working_dir: /project
    # run as root, default is uid 1000
    # user: 0:0
    user: 1000:1000
    environment:
      - HOME=/tmp
    volumes:
      - ./project:/project